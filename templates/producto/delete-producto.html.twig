{% extends 'back.html.twig' %}

{% block title %}Juan Bisquert | Peluqueria{% endblock %}

{% block body %}

    <div class="container-fluid">
        <div class="row justify-content-center">


            <!--MESSAGE

              <div class="alert alerta-login" role="alert">


              </div>

              -->


            <div class="card col-lg-4 col-md-6 py-5">
                <div class="card col-lg-3 col-md-6 py-2">
                    <img class="rounded w-100" src="{{asset(pictures_public_directory ~ producto.imagen)}}" alt="poster">
                </div>
                <input type="hidden" name="id" value="{{  producto.id }}">
                <h2><strong>Nombre:</strong>{{  producto.nombre }}</h2>
                <h2><strong>Categorioa:</strong>{{  producto.categoria }}</h2>
                <h2 class="card-title"><strong>Precio:</strong>{{  producto.precio }}</h2>
                <h2 class="text-muted mt-2"><strong>Descripcion:</strong>{{  producto.descripcion }}</h2>

            </div>

            <div class="row justify-content-center">
                <div class="col-4 col-lg-4 col-md-6">

                    <h2>¿Quieres borrar este usuario?</h2>

                    <a href="delete/yes"><button class="btn btn-danger">Si</button></a>
                    <a href="{{ path('admin') }}"><button class="btn btn-primary">No</button></a>


                </div>
            </div>
        </div>
    </div>
{% endblock %}