{% extends 'base.html.twig' %}

{% block title %}Tienda | Juan Bisquert{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-2">
                <div class="sidenav">
                    <a href="/tienda">Inicio</a>
                    <button class="dropdown-btn">Categorias
                        <i class="fa fa-caret-down"></i>
                    </button>
                    <div class="dropdown-container">
                        <a href="{{ path('categoria_champu') }}">Champús</a>
                        <a href="{{ path('categoria_tratamiento') }}">Tratamientos</a>
                        <a href="{{ path('categoria_accesorio') }}">Accesorios</a>
                    </div>

                </div>
            </div>
            <div class="col-1">

            </div>
        </div>
        <div class="row fila-texto-tienda">
            <div class="col-7 col-texto-tienda">
                <p>Tenemos los mejores productos de las mejores marcas. Disfrute de una amplia gama de productos de alta
                    calidad al mejor precio.</p>
            </div>
        </div>
    </div>

    <div class="container-fluid">
        <div class="row titulo-miniaturas-tienda">
            <div class="col-11">
                <h2>Tratamientos</h2>
                <hr>
                <div class="row productos-miniaturas">
                    {% if productos %}
                        {% for producto in productos %}
                            <div class="col-2 miniatura">
                                <img class="mt-3" src="{{ asset(pictures_public_directory ~  producto.imagen)}}" height="200">
                                <span>{{producto.precio}}</span>
                                <h2 class="nombre-producto">{{producto.nombre}}</h2>
                                <p class="producto-card-text">{{producto.descripcion}}</p>

                                <div class="row justify-content-center">
                                    <a href="tienda/{{producto.id}}/show"><button class="button-three"><i class="fas fa-info-circle"></i>Ver detalles</button></a>
                                </div>
                                <div class="row justify-content-center">
                                    <button class="button-three"><i class="fas fa-shopping-cart"></i>Añadir compra</button>
                                </div>
                            </div>
                        {% endfor %}

                    {% else %}
                        <h2>No hay productos disponibles</h2>
                    {% endif %}


                </div>
            </div>
        </div>
        <div class="container-fluid">
            <div class="row justify-content-center">
                <div class="col-1">
                    <nav aria-label="Page navigation example">
                        <ul class="pagination">
                            {% for i in 1 .. paginas %}
                                <li class="page-item"><a style="color: black" class="page-link active p-3"
                                                         href="/tienda/categoria-champu?page={{i}}">{{i}}</a></li>
                            {% endfor %}
                        </ul>
                    </nav>

                </div>
            </div>
        </div>
    </div>
{% endblock %}

