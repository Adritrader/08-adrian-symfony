{% extends 'base.html.twig' %}

{% block title %}Juan Bisquert | Peluqueria{% endblock %}

{% block body %}

    <div class="container-fluid">
        <div class="row justify-content-center">


            <!--MESSAGE

              <div class="alert alerta-login" role="alert">


              </div>

              -->


            <div class="card col-lg-3 col-md-3 mx-2 mt-5">
                <div class="card-header">
                    <h2>Reserva</h2>
                </div>
                <h3 class="card-title ml-5 mt-2"><strong>Username:</strong> {{reserva.usuario.username}}</h3>
                <hr>
                <h3 class="card-title ml-5"><strong>Servicio:</strong>{{reserva.ServicioId.nombre}}</h3>
                <hr>
                <h3 class="card-title ml-5"><strong>Hora:</strong>{{reserva.horaCita}}</h3>
                <hr>
                <h3 class="card-title ml-5"><em><strong>Fecha:</strong>{{  reserva.fechaCita | date("Y-m-d") }}</h3>
            </div>
        </div>
            <div class="row justify-content-center">
                <div class="col-3 col-lg-3 col-md-3">

                    <h2 class="mb-4 mt-2">¿Quieres borrar esta reserva?</h2>

                    <a href="{{ path('reservas_destroy', {id: reserva.id}) }}"><button class="btn btn-danger">Si</button></a>
                    <a href="{{ path('reservas_show', {id: app.user.id}) }}"><button class="btn btn-primary">No</button></a>


                </div>
            </div>
        </div>
    </div>
{% endblock %}