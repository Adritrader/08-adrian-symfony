{% extends 'back.html.twig' %}

{% block title %}Juan Bisquert | Peluqueria{% endblock %}
{% block body %}
    <div class="container-fluid">

        <div class="container-fluid py-4">

            <div class="row justify-content-center mt-2 mr-5">
                <div class="col-1 mr-5">
                    <a href="{{ path('admin_usuarios') }}"><button class="button-four bg-danger"><i class="fas fa-chevron-circle-left mx-1"></i>Atrás</button></a></div>

                <div class="col-3 py-3 bg-light">
                    <h1 class="px-3 mb-5">Editar usuario</h1>
                    <hr>
                    {{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}