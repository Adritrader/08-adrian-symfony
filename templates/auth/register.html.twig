{% extends 'base.html.twig' %}

{% block title %}Juan Bisquert | Peluqueria{% endblock %}
{% block body %}
    <div class="container-fluid">
        <div class="row fila-ruta">
            <div class="col-5 col-ruta">
                <div><span>Estas aquí: </span><a href="/">Home</a><span><i
                                class="fa fa-caret-right"></i><a href="/register"
                                                                 class="active">Register</a></span><span></span></div>
            </div>
        </div>
    </div>
    <div class="container-fluid py-4">
        <div class="row justify-content-center mt-2">
            <div class="col-3 py-3 bg-light">
                <h1 class="px-3 mb-3">Nuevo usuario</h1>
        {{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        </div>
        </div>
    </div>

{% endblock %}